PORT=4000
CORS_ORIGIN=http://localhost:5174
NODE_ENV=development

# Oturum imzalama anahtarı — üretimde en az 32 karakter zorunlu.
# Üretmek için: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=
JWT_EXPIRES=8h
JWT_COOKIE_MS=28800000

DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=etkinlig
DB_CONNECTION_LIMIT=10
